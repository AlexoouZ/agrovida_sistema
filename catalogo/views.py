from django.shortcuts import render
from . import data
# Create your views here.

def mostrar_productos(productos):
    nombre = 
    "productos" = {
        "nombre":nombre
    }
    return render(productos,'catalogo/index.html',"productos")
